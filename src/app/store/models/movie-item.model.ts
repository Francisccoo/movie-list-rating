 export interface MovieItem {
//Data model for the JSON

//We manage three parameters in every JSON element in the Array: id, movie title, and it's value rate
 	id: string;
	title: string;
	rate: number;

 }